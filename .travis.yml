language: node_js

node_js:
  - "6.0"

script:
  - echo "Deploy"

install:
  - npm install -g firebase-tools

after_success:
  - npm install
  - firebase deploy --token $FIREBASE_TOKEN

